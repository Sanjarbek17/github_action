name: My Action

on:
  push:
    branches: [ main ]


jobs:
  my-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print issue number
        run: echo "Issue number ${{ toJSON(github.event) }}"